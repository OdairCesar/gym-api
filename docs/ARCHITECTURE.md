# 🏗️ Arquitetura e Fluxograma - Gym API

Documentação visual da arquitetura e fluxos da aplicação.

---

## 📐 Arquitetura Geral

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENTE                              │
│                    (Mobile/Web App)                          │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTPS
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    NGINX (Reverse Proxy)                     │
│  • SSL/TLS Termination                                       │
│  • Rate Limiting (Adicional)                                 │
│  • Load Balancing                                            │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                   GYM API (AdonisJS)                         │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              MIDDLEWARE LAYER                        │   │
│  │  • ForceJsonResponse                                 │   │
│  │  • ContainerBindings                                 │   │
│  │  • Auth (Bearer Token)                               │   │
│  │  • Rate Limiting (@adonisjs/limiter)                 │   │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────▼─────────────────────────────────┐   │
│  │              ROUTES LAYER                            │   │
│  │  • /auth/*                                           │   │
│  │  • /users/*                                          │   │
│  │  • /diets/*  /trainings/*                            │   │
│  │  • /products/*  /gyms/*                              │   │
│  │  • /gym-permissions/*  /user-permissions/*           │   │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────▼─────────────────────────────────┐   │
│  │            CONTROLLERS LAYER                         │   │
│  │  • AuthController                                    │   │
│  │  • UsersController                                   │   │
│  │  • DietsController, MealsController, FoodsController │   │
│  │  • TrainingsController, ExercisesController          │   │
│  │  • ProductsController, GymsController                │   │
│  │  • GymPermissionsController, UserPermissionsController│  │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────▼─────────────────────────────────┐   │
│  │           AUTHORIZATION LAYER                        │   │
│  │  • Bouncer (Policy-based)                            │   │
│  │  • UserPolicy, DietPolicy, TrainingPolicy            │   │
│  │  • ProductPolicy, GymPolicy, PermissionPolicy        │   │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────▼─────────────────────────────────┐   │
│  │              MODELS LAYER                            │   │
│  │  • User, Gym, AccessToken                            │   │
│  │  • Diet, Meal, Food                                  │   │
│  │  • Training, Exercise (many-to-many)                 │   │
│  │  • Product, GymPermission, UserPermission            │   │
│  └────────────────────┬─────────────────────────────────┘   │
│                       │                                      │
│  ┌────────────────────▼─────────────────────────────────┐   │
│  │            SERVICES LAYER                            │   │
│  │  • PermissionService (cross-tenant logic)            │   │
│  │  • ErrorMonitoringService (Sentry/None)              │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    MySQL Database                            │
│  • Multi-tenant (logical isolation via gym_id)               │
│  • Tables: users, gyms, diets, trainings, products, etc.     │
│  • Rate limiting table                                       │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                  EXTERNAL SERVICES                            │
│  • Sentry (Error Monitoring) - Opcional                       │
│  • Redis (Cache - Futuro) - Opcional                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔐 Fluxo de Autenticação

```
┌──────────┐                                              ┌──────────┐
│  Client  │                                              │   API    │
└────┬─────┘                                              └────┬─────┘
     │                                                          │
     │  POST /auth/register                                    │
     │  {email, password, name, gym_id, role}                  │
     ├─────────────────────────────────────────────────────────▶
     │                                                          │
     │                      ┌─────────────────────┐            │
     │                      │ 1. Validate Data    │            │
     │                      │ 2. Hash Password    │            │
     │                      │ 3. Create User      │            │
     │                      └─────────────────────┘            │
     │                                                          │
     │  201 Created                                             │
     │  {user: {...}, token: {...}}                            │
     ◀─────────────────────────────────────────────────────────┤
     │                                                          │
     │  POST /auth/login                                        │
     │  {email, password}                                       │
     ├─────────────────────────────────────────────────────────▶
     │                                                          │
     │                      ┌─────────────────────┐            │
     │                      │ 1. Rate Limit Check │            │
     │                      │    (5 req/min)      │            │
     │                      │ 2. Find User        │            │
     │                      │ 3. Verify Password  │            │
     │                      │ 4. Generate Token   │            │
     │                      └─────────────────────┘            │
     │                                                          │
     │  200 OK                                                  │
     │  {user: {...}, token: {token, type: 'bearer'}}          │
     ◀─────────────────────────────────────────────────────────┤
     │                                                          │
     │  GET /auth/me                                            │
     │  Header: Authorization: Bearer <token>                   │
     ├─────────────────────────────────────────────────────────▶
     │                                                          │
     │                      ┌─────────────────────┐            │
     │                      │ 1. Verify Token     │            │
     │                      │ 2. Load User        │            │
     │                      │ 3. Return User Data │            │
     │                      └─────────────────────┘            │
     │                                                          │
     │  200 OK                                                  │
     │  {user: {...}}                                          │
     ◀─────────────────────────────────────────────────────────┤
     │                                                          │
     │  POST /auth/logout                                       │
     │  Header: Authorization: Bearer <token>                   │
     ├─────────────────────────────────────────────────────────▶
     │                                                          │
     │                      ┌─────────────────────┐            │
     │                      │ 1. Verify Token     │            │
     │                      │ 2. Delete Token     │            │
     │                      │    from Database    │            │
     │                      └─────────────────────┘            │
     │                                                          │
     │  200 OK                                                  │
     │  {message: 'Logged out successfully'}                   │
     ◀─────────────────────────────────────────────────────────┤
     │                                                          │
```

---

## 🏋️ Fluxo de Criação de Treino (Multi-tenant)

```
┌──────────┐                                              ┌──────────┐
│ Personal │                                              │   API    │
└────┬─────┘                                              └────┬─────┘
     │                                                          │
     │  POST /trainings                                         │
     │  Header: Authorization: Bearer <token>                   │
     │  Body: {name, client_id, gym_id, observations}           │
     ├─────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Auth Middleware             │          │
     │              │    • Verify Token              │          │
     │              │    • Load User (Personal)      │          │
     │              │                                │          │
     │              │ 2. Rate Limiting               │          │
     │              │    • Check: 100 req/min        │          │
     │              │                                │          │
     │              │ 3. Validator                   │          │
     │              │    • Required fields           │          │
     │              │    • Data types                │          │
     │              │                                │          │
     │              │ 4. Controller                  │          │
     │              │    • TrainingsController       │          │
     │              │                                │          │
     │              │ 5. Policy Check                │          │
     │              │    • Can create training?      │          │
     │              │    • Same gym as personal?     │          │
     │              │    • Client exists in gym?     │          │
     │              │                                │          │
     │              │ 6. Database                    │          │
     │              │    • INSERT INTO trainings     │          │
     │              │    • Multi-tenant via gym_id   │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  201 Created                                             │
     │  {id, name, client_id, gym_id, ...}                     │
     ◀─────────────────────────────────────────────────────────┤
     │                                                          │
     │  POST /trainings/:id/exercises                           │
     │  Body: {exercise_id, series, repetitions, weight, ...}   │
     ├─────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Verify ownership            │          │
     │              │    • Training belongs to gym?  │          │
     │              │                                │          │
     │              │ 2. Insert Pivot Table          │          │
     │              │    • exercise_training table   │          │
     │              │    • Custom data: series, reps │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  200 OK                                                  │
     │  {training: {...}, exercises: [...]}                    │
     ◀─────────────────────────────────────────────────────────┤
     │                                                          │
```

---

## 🥗 Fluxo de Dietas (Nested Resources)

```
Diet (Dieta)
  │
  ├─── Meal (Refeição 1) - Café da Manhã
  │      ├─── Food (Alimento 1) - Aveia
  │      ├─── Food (Alimento 2) - Banana
  │      └─── Food (Alimento 3) - Mel
  │
  ├─── Meal (Refeição 2) - Almoço
  │      ├─── Food (Alimento 1) - Arroz
  │      ├─── Food (Alimento 2) - Frango
  │      └─── Food (Alimento 3) - Salada
  │
  └─── Meal (Refeição 3) - Jantar
         ├─── Food (Alimento 1) - Peixe
         └─── Food (Alimento 2) - Legumes

Rotas:
POST   /diets                          # Criar dieta
POST   /diets/:dietId/meals            # Criar refeição na dieta
POST   /meals/:mealId/foods            # Criar alimento na refeição
GET    /diets/:id                      # Ver dieta completa (eager loading)
```

---

## 🔑 Fluxo de Permissões Cross-Tenant

```
┌──────────────────────────────────────────────────────────────┐
│                    CENÁRIO 1: GYM PERMISSION                  │
└──────────────────────────────────────────────────────────────┘

Academia A                              Academia B
(owner_gym_id: 1)                       (granted_gym_id: 2)
     │                                        │
     └────── concede permissão ──────────────▶
                 personal_id: 5
                 can_edit_diets: true
                 can_edit_trainings: true

Resultado:
• Personal (id: 5) pode acessar recursos da Academia B
• Pode criar/editar treinos e dietas para clientes da Academia B
• Consulta: PermissionService.canAccessGymResource()


┌──────────────────────────────────────────────────────────────┐
│                   CENÁRIO 2: USER PERMISSION                  │
└──────────────────────────────────────────────────────────────┘

Cliente (user_id: 10, gym_id: 1)
     │
     └────── concede permissão ──────────────▶  Personal (grantee_id: 15)
                 grantee_type: 'user'
                 can_edit_diet: true
                 can_edit_training: true

Resultado:
• Personal (id: 15) pode editar DIETA e TREINO do Cliente (id: 10)
• Permissão granular: pode ser configurada individualmente
• Consulta: PermissionService.canEditUserResource()


Fluxo de Verificação:
┌──────────┐      ┌──────────┐      ┌──────────────┐      ┌──────────┐
│ Request  │ ───▶ │  Policy  │ ───▶ │ Permission   │ ───▶ │ Database │
│          │      │          │      │   Service    │      │          │
└──────────┘      └──────────┘      └──────────────┘      └──────────┘
                       │                    │
                       │                    ├─ Check GymPermission
                       │                    ├─ Check UserPermission
                       │                    └─ Return boolean
                       │
                       └─ Allow/Deny access
```

---

## 🚦 Fluxo de Rate Limiting

```
Client Request
     │
     ▼
┌─────────────────────────────────────────┐
│   NGINX Rate Limiting (Opcional)         │
│   • Primeiro nível de proteção           │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   AdonisJS Limiter Middleware            │
│                                          │
│   1. Identifica chave:                   │
│      • IP (não autenticado)              │
│      • user_{id} (autenticado)           │
│                                          │
│   2. Consulta database:                  │
│      SELECT * FROM rate_limits           │
│      WHERE key = ? AND expire_at > NOW() │
│                                          │
│   3. Verifica limite:                    │
│      • authThrottle: 5 req/min           │
│      • apiThrottle: 100 req/min          │
│      • publicThrottle: 30 req/min        │
│                                          │
│   4. Decisão:                            │
│      ├─ Dentro do limite ──▶ Continua    │
│      │                                    │
│      └─ Excedeu limite ──▶ 429 Response  │
│                                          │
└─────────────────────────────────────────┘

Resposta 429:
{
  "errors": [{
    "message": "Too many requests"
  }]
}

Headers:
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 0
X-RateLimit-Reset: 1645123456
Retry-After: 45
```

---

## 🐛 Fluxo de Error Monitoring

```
Exception Ocorre
     │
     ▼
┌─────────────────────────────────────────┐
│   HttpExceptionHandler                   │
│                                          │
│   report(error, ctx) {                   │
│                                          │
│     1. Captura contexto:                 │
│        • Usuário autenticado             │
│        • Método e URL da requisição      │
│        • IP e User-Agent                 │
│        • Environment                     │
│                                          │
│     2. Remove dados sensíveis:           │
│        • Authorization headers           │
│        • Cookies                         │
│                                          │
│     3. Envia para Error Monitoring:      │
│        errorMonitoring.captureException()│
│   }                                      │
│                                          │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   ErrorMonitoringFactory                 │
│                                          │
│   provider = 'sentry' or 'none'          │
│                                          │
│   ├─ SentryMonitoringService             │
│   │   • Envia para Sentry.io             │
│   │   • Agrega erros similares           │
│   │   • Contexto rico                    │
│   │                                      │
│   └─ NullMonitoringService               │
│       • No-op (desenvolvimento/testes)   │
│                                          │
└─────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   Sentry Dashboard                       │
│   • Visualizar erros                     │
│   • Notificações                         │
│   • Stack traces                         │
│   • Agregação inteligente                │
└─────────────────────────────────────────┘
```

---

## � Sistema de Moderação de Usuários

### Hierarquia e Tipos de Usuário

```
┌────────────────────────────────────────────────────────────┐
│                     SUPER USER                             │
│  • Gerencia o sistema globalmente                          │
│  • Pode criar gyms                                         │
│  • Pode aprovar qualquer usuário de qualquer gym           │
│  • Sempre pode fazer login (não precisa de aprovação)      │
│  • Só pode ser criado diretamente no banco de dados        │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                         ADMIN                              │
│  • Gerencia uma gym específica                             │
│  • Pode aprovar usuários da sua gym                        │
│  • Pode criar usuários já aprovados na sua gym             │
│  • Precisa ser aprovado para fazer login                   │
│  • Isolamento por gym_id                                   │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                       PERSONAL                             │
│  • Pode aprovar usuários da sua gym                        │
│  • Pode criar usuários já aprovados na sua gym             │
│  • Gerencia treinos e dietas                               │
│  • Precisa ser aprovado para fazer login                   │
│  • Isolamento por gym_id                                   │
└────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────┐
│                        CLIENTE                             │
│  • Usuário final do sistema                                │
│  • Acessa seus próprios treinos e dietas                   │
│  • Precisa ser aprovado para fazer login                   │
│  • Isolamento por gym_id                                   │
└────────────────────────────────────────────────────────────┘
```

### Fluxo de Moderação

```
┌──────────┐                                              ┌──────────┐
│  Novo    │                                              │   API    │
│ Usuário  │                                              └────┬─────┘
└────┬─────┘                                                   │
     │                                                          │
     │  POST /auth/register                                    │
     │  {name, email, password, gymId, isAdmin, isPersonal}    │
     ├────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Validar dados               │          │
     │              │ 2. Hash password               │          │
     │              │ 3. Create user com:            │          │
     │              │    • approved = false          │          │
     │              │    • is_super = false          │          │
     │              │ 4. Gerar token                 │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  201 Created                                             │
     │  {message: "Access pending approval", user, token}      │
     ◀────────────────────────────────────────────────────────┤
     │                                                          │
     │                                                          │
     │  POST /auth/login                                        │
     │  {email, password}                                       │
     ├────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Verificar credenciais       │          │
     │              │ 2. Chamar user.canLogin()      │          │
     │              │    • Se is_super: true         │          │
     │              │    • Senão: verificar approved │          │
     │              │ 3. Se não aprovado:            │          │
     │              │    • Retornar 403 Forbidden    │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  403 Forbidden                                           │
     │  {message: "Access pending approval. Contact admin"}    │
     ◀────────────────────────────────────────────────────────┤
     │                                                          │


┌──────────┐                                              ┌──────────┐
│  Admin/  │                                              │   API    │
│ Personal │                                              └────┬─────┘
└────┬─────┘                                                   │
     │                                                          │
     │  GET /users/pending-users                               │
     │  Header: Authorization: Bearer <token>                  │
     ├────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Verificar se é admin/       │          │
     │              │    personal aprovado ou super  │          │
     │              │ 2. Filtrar por gym_id          │          │
     │              │    (exceto super users)        │          │
     │              │ 3. WHERE approved = false      │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  200 OK                                                  │
     │  {data: [{id, name, email, gym_id, ...}]}               │
     ◀────────────────────────────────────────────────────────┤
     │                                                          │
     │                                                          │
     │  POST /users/{id}/approve-user                          │
     │  Header: Authorization: Bearer <token>                  │
     ├────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Verificar permissões        │          │
     │              │    • Super: pode aprovar todos │          │
     │              │    • Admin/Personal aprovado:  │          │
     │              │      apenas da mesma gym       │          │
     │              │ 2. Verificar se pendente       │          │
     │              │ 3. UPDATE users SET:           │          │
     │              │    • approved = true           │          │
     │              │    • approved_by = current_id  │          │
     │              │    • approved_at = NOW()       │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  200 OK                                                  │
     │  {message: "User approved", data: {...}}                │
     ◀────────────────────────────────────────────────────────┤
     │                                                          │
     │                                                          │
     │  POST /users/{id}/reject-user                           │
     │  Header: Authorization: Bearer <token>                  │
     ├────────────────────────────────────────────────────────▶
     │                                                          │
     │              ┌────────────────────────────────┐          │
     │              │ 1. Verificar permissões        │          │
     │              │ 2. Verificar se pendente       │          │
     │              │ 3. DELETE user                 │          │
     │              └────────────────────────────────┘          │
     │                                                          │
     │  200 OK                                                  │
     │  {message: "User rejected and deleted"}                 │
     ◀────────────────────────────────────────────────────────┤
```

### Regras de Aprovação

| Criador              | User Criado | Status Inicial |
|---------------------|-------------|----------------|
| Sistema (register)   | Qualquer    | `approved = false` |
| Super User          | Qualquer    | `approved = true` |
| Admin aprovado      | Qualquer    | `approved = true` |
| Personal aprovado   | Qualquer    | `approved = true` |
| Admin não aprovado  | Qualquer    | `approved = false` |
| Personal não aprovado| Qualquer   | `approved = false` |

---

## �💾 Modelo de Dados (Relacionamentos)

```
┌──────────┐
│   Gym    │
└────┬─────┘
     │ 1:N
     │
     ├─────────────────┬─────────────────┬─────────────────┐
     │                 │                 │                 │
     ▼                 ▼                 ▼                 ▼
┌─────────┐      ┌─────────┐      ┌──────────┐     ┌──────────┐
│  User   │      │ Product │      │   Diet   │     │ Training │
└────┬────┘      └─────────┘      └────┬─────┘     └────┬─────┘
     │                                  │                 │
     │ 1:N                              │ 1:N             │ 1:N
     │                                  │                 │
     ├──────────────┐                   ▼                 ▼
     │              │             ┌─────────┐       ┌──────────┐
     ▼              ▼             │  Meal   │       │ Exercise │
┌─────────┐  ┌──────────────┐    └────┬────┘       └────┬─────┘
│  Diet   │  │   Training   │         │                  │
└─────────┘  └──────────────┘         │ 1:N              │ N:M
                                       │                  │
                                       ▼                  ▼
                                 ┌─────────┐    ┌─────────────────┐
                                 │  Food   │    │ exercise_training│
                                 └─────────┘    │  (Pivot Table)  │
                                                └─────────────────┘

Permissões Cross-Tenant:

┌─────────────────┐                    ┌─────────────────┐
│ GymPermission   │                    │ UserPermission  │
├─────────────────┤                    ├─────────────────┤
│ owner_gym_id    │                    │ user_id         │
│ granted_gym_id  │                    │ grantee_type    │
│ personal_id     │                    │ grantee_id      │
│ can_edit_diets  │                    │ can_edit_diet   │
│ can_edit_trainings│                  │ can_edit_training│
│ is_active       │                    │ is_active       │
└─────────────────┘                    └─────────────────┘

Multi-tenant: Todas as tabelas principais têm gym_id
```

---

## 🔄 Ciclo de Vida de uma Requisição

```
1. Client envia requisição
         │
         ▼
2. NGINX (Reverse Proxy)
   • SSL termination
   • Load balancing
         │
         ▼
3. AdonisJS Router
   • Match route pattern
         │
         ▼
4. Middleware Stack
   • ForceJsonResponse
   • ContainerBindings
   • Rate Limiting
   • Authentication
         │
         ▼
5. Validator
   • Valida dados de entrada
         │
         ▼
6. Controller
   • Lógica de negócio
         │
         ▼
7. Policy (Bouncer)
   • Verifica permissões
   • Multi-tenant checks
         │
         ▼
8. Service Layer (se necessário)
   • PermissionService
   • Business logic complexa
         │
         ▼
9. Model (Lucid ORM)
   • Query database
   • Relationships
         │
         ▼
10. Database (MySQL)
    • Execute query
    • Return results
         │
         ▼
11. Transform Response
    • Serialização
    • JSON formatting
         │
         ▼
12. Send Response
    • Status code
    • Headers
    • Body
         │
         ▼
13. Client recebe resposta

── Se erro ocorrer em qualquer ponto ──
         │
         ▼
14. Exception Handler
    • Log error
    • Send to Sentry
    • Return error response
```

---

## 🎯 Principais Características

### Multi-tenancy
- Isolamento lógico via `gym_id`
- Todas queries filtradas por academia
- Cross-tenant via permissões explícitas

### Segurança
- Rate limiting em múltiplos níveis
- Policy-based authorization
- Token authentication
- Senha hasheada (bcrypt)

### Observability
- Error monitoring (Sentry)
- Structured logs
- Health check endpoint

### Performance
- Eager loading de relacionamentos
- Índices no banco
- Connection pooling
- Cluster mode (PM2)

---

**Última atualização:** 16/02/2026  
**Versão:** 1.0
